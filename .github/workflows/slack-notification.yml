name: Daily Slack Notification

on:
  schedule:
    # 매일 오전 8시 (UTC 23:00, 한국 시간 8:00)
    - cron: '0 23 * * *'
  workflow_dispatch: # 수동 실행 가능

jobs:
  send-slack-notification:
    runs-on: ubuntu-latest
    
    steps:
    - name: Send daily Slack notification
      run: |
        echo "Sending daily Slack notification..."
        curl -X POST https://lumina-next-picker.vercel.app/api/slack/daily-notification
        echo "Daily notification sent!"
        
    - name: Notify completion
      run: |
        echo "✅ Daily Slack notification completed at $(date)"
